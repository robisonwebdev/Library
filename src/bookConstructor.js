class book {
    constructor(author, title, pages, status, dataKey) {
        this.author = author;
        this.title = title;
        this.pages = pages;
        this.status = status;
        this.dataKey = dataKey;
    }
}

export default book;